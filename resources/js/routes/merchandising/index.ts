import purchaseOrders from './purchase-orders'
import bqs from './bqs'
const merchandising = {
    purchaseOrders: Object.assign(purchaseOrders, purchaseOrders),
bqs: Object.assign(bqs, bqs),
}

export default merchandising